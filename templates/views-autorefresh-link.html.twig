{#
/**
 * @file
 * Default theme implementation to display a views autorefresh link.
 *
 * Available variables:
 *   - view: The view object of the view the refresh link is rendered in.
 *   - interval: The period between refreshes
 *   - timestamp
 *   - ping
 *   - incremental
 *   - nodejs
 *
 * @see template_preprocess_views_autorefresh_link()
 *
 * @ingroup themeable
 */
#}
{{ content }}
